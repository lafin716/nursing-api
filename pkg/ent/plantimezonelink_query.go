// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"nursing_api/pkg/ent/plantimezonelink"
	"nursing_api/pkg/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// PlanTimeZoneLinkQuery is the builder for querying PlanTimeZoneLink entities.
type PlanTimeZoneLinkQuery struct {
	config
	ctx        *QueryContext
	order      []plantimezonelink.OrderOption
	inters     []Interceptor
	predicates []predicate.PlanTimeZoneLink
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlanTimeZoneLinkQuery builder.
func (ptzlq *PlanTimeZoneLinkQuery) Where(ps ...predicate.PlanTimeZoneLink) *PlanTimeZoneLinkQuery {
	ptzlq.predicates = append(ptzlq.predicates, ps...)
	return ptzlq
}

// Limit the number of records to be returned by this query.
func (ptzlq *PlanTimeZoneLinkQuery) Limit(limit int) *PlanTimeZoneLinkQuery {
	ptzlq.ctx.Limit = &limit
	return ptzlq
}

// Offset to start from.
func (ptzlq *PlanTimeZoneLinkQuery) Offset(offset int) *PlanTimeZoneLinkQuery {
	ptzlq.ctx.Offset = &offset
	return ptzlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ptzlq *PlanTimeZoneLinkQuery) Unique(unique bool) *PlanTimeZoneLinkQuery {
	ptzlq.ctx.Unique = &unique
	return ptzlq
}

// Order specifies how the records should be ordered.
func (ptzlq *PlanTimeZoneLinkQuery) Order(o ...plantimezonelink.OrderOption) *PlanTimeZoneLinkQuery {
	ptzlq.order = append(ptzlq.order, o...)
	return ptzlq
}

// First returns the first PlanTimeZoneLink entity from the query.
// Returns a *NotFoundError when no PlanTimeZoneLink was found.
func (ptzlq *PlanTimeZoneLinkQuery) First(ctx context.Context) (*PlanTimeZoneLink, error) {
	nodes, err := ptzlq.Limit(1).All(setContextOp(ctx, ptzlq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{plantimezonelink.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) FirstX(ctx context.Context) *PlanTimeZoneLink {
	node, err := ptzlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlanTimeZoneLink ID from the query.
// Returns a *NotFoundError when no PlanTimeZoneLink ID was found.
func (ptzlq *PlanTimeZoneLinkQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ptzlq.Limit(1).IDs(setContextOp(ctx, ptzlq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{plantimezonelink.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ptzlq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlanTimeZoneLink entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlanTimeZoneLink entity is found.
// Returns a *NotFoundError when no PlanTimeZoneLink entities are found.
func (ptzlq *PlanTimeZoneLinkQuery) Only(ctx context.Context) (*PlanTimeZoneLink, error) {
	nodes, err := ptzlq.Limit(2).All(setContextOp(ctx, ptzlq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{plantimezonelink.Label}
	default:
		return nil, &NotSingularError{plantimezonelink.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) OnlyX(ctx context.Context) *PlanTimeZoneLink {
	node, err := ptzlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlanTimeZoneLink ID in the query.
// Returns a *NotSingularError when more than one PlanTimeZoneLink ID is found.
// Returns a *NotFoundError when no entities are found.
func (ptzlq *PlanTimeZoneLinkQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ptzlq.Limit(2).IDs(setContextOp(ctx, ptzlq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{plantimezonelink.Label}
	default:
		err = &NotSingularError{plantimezonelink.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ptzlq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlanTimeZoneLinks.
func (ptzlq *PlanTimeZoneLinkQuery) All(ctx context.Context) ([]*PlanTimeZoneLink, error) {
	ctx = setContextOp(ctx, ptzlq.ctx, "All")
	if err := ptzlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PlanTimeZoneLink, *PlanTimeZoneLinkQuery]()
	return withInterceptors[[]*PlanTimeZoneLink](ctx, ptzlq, qr, ptzlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) AllX(ctx context.Context) []*PlanTimeZoneLink {
	nodes, err := ptzlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlanTimeZoneLink IDs.
func (ptzlq *PlanTimeZoneLinkQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ptzlq.ctx.Unique == nil && ptzlq.path != nil {
		ptzlq.Unique(true)
	}
	ctx = setContextOp(ctx, ptzlq.ctx, "IDs")
	if err = ptzlq.Select(plantimezonelink.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ptzlq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ptzlq *PlanTimeZoneLinkQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ptzlq.ctx, "Count")
	if err := ptzlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ptzlq, querierCount[*PlanTimeZoneLinkQuery](), ptzlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) CountX(ctx context.Context) int {
	count, err := ptzlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ptzlq *PlanTimeZoneLinkQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ptzlq.ctx, "Exist")
	switch _, err := ptzlq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ptzlq *PlanTimeZoneLinkQuery) ExistX(ctx context.Context) bool {
	exist, err := ptzlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlanTimeZoneLinkQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ptzlq *PlanTimeZoneLinkQuery) Clone() *PlanTimeZoneLinkQuery {
	if ptzlq == nil {
		return nil
	}
	return &PlanTimeZoneLinkQuery{
		config:     ptzlq.config,
		ctx:        ptzlq.ctx.Clone(),
		order:      append([]plantimezonelink.OrderOption{}, ptzlq.order...),
		inters:     append([]Interceptor{}, ptzlq.inters...),
		predicates: append([]predicate.PlanTimeZoneLink{}, ptzlq.predicates...),
		// clone intermediate query.
		sql:  ptzlq.sql.Clone(),
		path: ptzlq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PrescriptionID uuid.UUID `json:"prescription_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlanTimeZoneLink.Query().
//		GroupBy(plantimezonelink.FieldPrescriptionID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ptzlq *PlanTimeZoneLinkQuery) GroupBy(field string, fields ...string) *PlanTimeZoneLinkGroupBy {
	ptzlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PlanTimeZoneLinkGroupBy{build: ptzlq}
	grbuild.flds = &ptzlq.ctx.Fields
	grbuild.label = plantimezonelink.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PrescriptionID uuid.UUID `json:"prescription_id,omitempty"`
//	}
//
//	client.PlanTimeZoneLink.Query().
//		Select(plantimezonelink.FieldPrescriptionID).
//		Scan(ctx, &v)
func (ptzlq *PlanTimeZoneLinkQuery) Select(fields ...string) *PlanTimeZoneLinkSelect {
	ptzlq.ctx.Fields = append(ptzlq.ctx.Fields, fields...)
	sbuild := &PlanTimeZoneLinkSelect{PlanTimeZoneLinkQuery: ptzlq}
	sbuild.label = plantimezonelink.Label
	sbuild.flds, sbuild.scan = &ptzlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlanTimeZoneLinkSelect configured with the given aggregations.
func (ptzlq *PlanTimeZoneLinkQuery) Aggregate(fns ...AggregateFunc) *PlanTimeZoneLinkSelect {
	return ptzlq.Select().Aggregate(fns...)
}

func (ptzlq *PlanTimeZoneLinkQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ptzlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ptzlq); err != nil {
				return err
			}
		}
	}
	for _, f := range ptzlq.ctx.Fields {
		if !plantimezonelink.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ptzlq.path != nil {
		prev, err := ptzlq.path(ctx)
		if err != nil {
			return err
		}
		ptzlq.sql = prev
	}
	return nil
}

func (ptzlq *PlanTimeZoneLinkQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlanTimeZoneLink, error) {
	var (
		nodes = []*PlanTimeZoneLink{}
		_spec = ptzlq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlanTimeZoneLink).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlanTimeZoneLink{config: ptzlq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ptzlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ptzlq *PlanTimeZoneLinkQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ptzlq.querySpec()
	_spec.Node.Columns = ptzlq.ctx.Fields
	if len(ptzlq.ctx.Fields) > 0 {
		_spec.Unique = ptzlq.ctx.Unique != nil && *ptzlq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ptzlq.driver, _spec)
}

func (ptzlq *PlanTimeZoneLinkQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(plantimezonelink.Table, plantimezonelink.Columns, sqlgraph.NewFieldSpec(plantimezonelink.FieldID, field.TypeUUID))
	_spec.From = ptzlq.sql
	if unique := ptzlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ptzlq.path != nil {
		_spec.Unique = true
	}
	if fields := ptzlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, plantimezonelink.FieldID)
		for i := range fields {
			if fields[i] != plantimezonelink.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ptzlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ptzlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ptzlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ptzlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ptzlq *PlanTimeZoneLinkQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ptzlq.driver.Dialect())
	t1 := builder.Table(plantimezonelink.Table)
	columns := ptzlq.ctx.Fields
	if len(columns) == 0 {
		columns = plantimezonelink.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ptzlq.sql != nil {
		selector = ptzlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ptzlq.ctx.Unique != nil && *ptzlq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ptzlq.predicates {
		p(selector)
	}
	for _, p := range ptzlq.order {
		p(selector)
	}
	if offset := ptzlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ptzlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PlanTimeZoneLinkGroupBy is the group-by builder for PlanTimeZoneLink entities.
type PlanTimeZoneLinkGroupBy struct {
	selector
	build *PlanTimeZoneLinkQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ptzlgb *PlanTimeZoneLinkGroupBy) Aggregate(fns ...AggregateFunc) *PlanTimeZoneLinkGroupBy {
	ptzlgb.fns = append(ptzlgb.fns, fns...)
	return ptzlgb
}

// Scan applies the selector query and scans the result into the given value.
func (ptzlgb *PlanTimeZoneLinkGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptzlgb.build.ctx, "GroupBy")
	if err := ptzlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlanTimeZoneLinkQuery, *PlanTimeZoneLinkGroupBy](ctx, ptzlgb.build, ptzlgb, ptzlgb.build.inters, v)
}

func (ptzlgb *PlanTimeZoneLinkGroupBy) sqlScan(ctx context.Context, root *PlanTimeZoneLinkQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ptzlgb.fns))
	for _, fn := range ptzlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ptzlgb.flds)+len(ptzlgb.fns))
		for _, f := range *ptzlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ptzlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptzlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PlanTimeZoneLinkSelect is the builder for selecting fields of PlanTimeZoneLink entities.
type PlanTimeZoneLinkSelect struct {
	*PlanTimeZoneLinkQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ptzls *PlanTimeZoneLinkSelect) Aggregate(fns ...AggregateFunc) *PlanTimeZoneLinkSelect {
	ptzls.fns = append(ptzls.fns, fns...)
	return ptzls
}

// Scan applies the selector query and scans the result into the given value.
func (ptzls *PlanTimeZoneLinkSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ptzls.ctx, "Select")
	if err := ptzls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlanTimeZoneLinkQuery, *PlanTimeZoneLinkSelect](ctx, ptzls.PlanTimeZoneLinkQuery, ptzls, ptzls.inters, v)
}

func (ptzls *PlanTimeZoneLinkSelect) sqlScan(ctx context.Context, root *PlanTimeZoneLinkQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ptzls.fns))
	for _, fn := range ptzls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ptzls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ptzls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
